# Roadmap

## Done

- T001 - Freeze the v3 policy contract, authority boundaries, mode semantics, migration rules, non-goals, and protocol-layering decision (`AC-01` to `AC-03`).
- T002 - Pin policy identity/digest per flow; generate runtime schemas/enums; add `standard`/`strict` escalation, controlled-state digests and explicit migration (`AC-01` to `AC-04`).
- T003 - Replace the legacy MCP surface with seven high-level lifecycle operations and constrain evidence paths to `plan_root` (`AC-03`, `AC-04`, `AC-06`).
- T004 - Make host-plan projection advisory and add opt-in, non-writing Codex lifecycle Hook guidance plus a manual fallback (`AC-05`, `AC-07`).
- T005 - Run recovery, migration, controller, PowerShell, global-install and cross-Agent contract checks; archive reproducible evidence (`AC-08`).

## Deferred

- A mandatory hook-based security sandbox.
- Native plan integrations for hosts without verified public APIs.
