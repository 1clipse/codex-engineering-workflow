# Open Questions

- Q002 - What compatibility policy should apply when a future policy release removes a currently valid `strict` action class? Migration must remain explicit and fail closed until this is decided.
