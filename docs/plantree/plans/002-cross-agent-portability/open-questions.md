# Open Questions

- Which concrete products does the user mean by `dsh` and `zcode`? Until their host-owned configuration formats are verified, they receive a generic standard-MCP adapter with an explicit manual installation step.
