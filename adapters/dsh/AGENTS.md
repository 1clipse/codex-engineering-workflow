# Engineering Workflow

Read `plugins/delivery-control/schemas/workflow-policy.json` before a durable delivery. Use the seven high-level `delivery-control` MCP operations for transactional flow state and Plan Tree for business state. DSH does not need a native plan-confirmation handshake: host planning is optional session guidance, never a close gate.
