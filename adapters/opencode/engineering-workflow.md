---
description: Start a JSON-controlled delivery flow with Delivery Control and Plan Tree.
---

Read `plugins/delivery-control/schemas/workflow-policy.json` and `skills/engineering-workflow/references/host-capabilities.json`. Use the seven high-level `delivery-control` operations for all durable flow changes and keep business state in Plan Tree. A host plan is optional session guidance; its absence must not block the evidence-based close gate.
