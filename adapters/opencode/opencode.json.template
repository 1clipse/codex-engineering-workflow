{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "delivery-control": {
      "type": "local",
      "command": [
        "node",
        "{SERVER_PATH}"
      ],
      "cwd": "{PROJECT_ROOT}",
      "enabled": true
    }
  }
}
